[tox]
envlist = py, packaging

[testenv]
usedevelop = True
extras =
	test
commands =
	pytest {posargs}

[testenv:packaging]
deps =
	check-manifest
	readme-renderer
commands =
	check-manifest
	python setup.py check -m -r -s

# vim: commentstring=#\ %s list
